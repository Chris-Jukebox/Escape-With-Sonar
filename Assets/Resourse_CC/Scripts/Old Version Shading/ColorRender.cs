using UnityEngine;
using System.Collections;

public class ColorRender : MonoBehaviour {
    /*
    public GameObject renderObj;
    private RenderGroup renderGroup;

    private static Vector3 pos = new Vector3(0, 2.322f, 0.39f);

    void Start()
    {
        int index = GameManager.instance.renderCount++;
        pos.x += index * 1.6f;
        GameObject obj = (GameObject)Instantiate(renderObj, pos, Quaternion.identity);
        renderGroup = obj.GetComponent<RenderGroup>();
        renderGroup.Setup();
        GetComponent<Renderer>().material.mainTexture = renderGroup.tex;
    }

    public void AddColorPoint(Vector2 pixelUV, float value)
    {
        renderGroup.AddParticle(pixelUV, value);

    }*/

}
